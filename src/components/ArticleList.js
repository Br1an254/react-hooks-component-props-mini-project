import React from "react";

const ArticleList = ({articles})=>{

    const log = articles.map((article)=>{
    //    return ()
    })

    
    
    return(
        <main>
        </main>
    )
}


export default ArticleList